import MyAddressModalView from '@components/Account/MyAddressModal/views';

const MyAddressModalWidget = (props) => {
  return <MyAddressModalView {...props} />;
};

export default MyAddressModalWidget;
