// -- views
import VideoSection from '@components/VideoSection/views';

// -- data
import dummyData from '@components/VideoSection/data';

const VideoSectionWidget = () => {
  return <VideoSection data={dummyData} />;
};

export default VideoSectionWidget;
